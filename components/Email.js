const Email = () => {
  return (
    <div>
      <label htmlFor="email">Email Address:</label>
      <input
        type="email"
        id="email"
        name="email"
        required
        placeholder="sophie@example.com"
      />
    </div>
  )
}

export default Email
